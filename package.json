{
  "name": "data-router-standalone",
  "private": true,
  "version": "1.0.0",
  "description": "Root baseline validation for revvel-standards compliance.",
  "scripts": {
    "test": "node scripts/test-baseline.js",
    "build": "node scripts/build-baseline.js"
  }
}
